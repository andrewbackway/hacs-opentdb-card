{
  "name": "opentdb-card",
  "version": "0.2.9",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"require('fs').copyFileSync('dist/opentdb-card.js', 'opentdb-card.js'); require('fs').rmSync('dist', { recursive: true, force: true })\""
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
